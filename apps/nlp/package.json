{
  "name": "@ikan/nlp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload --port 8000",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "typecheck": "echo 'nlp is a python service; tsc skipped'",
    "lint": "echo 'nlp is a python service; eslint skipped'"
  },
  "devDependencies": {}
}
